"""The will's outward hands: governed delegated execution.

Not cognition (that is `engine/`), not low-level command primitives (`actions/`),
not action surfaces (`environments/`). This layer is how the will reaches OUT
through governed artifacts: R0 read-only delegation to a coding-harness worker
(`delegation`) and R1 patch drafting that is validated but never applied
(`patches`). Both spend the existence budget, pass the policy gate, and land as
semantic events — the governance core stays in `engine/`/`policy/`.
"""
